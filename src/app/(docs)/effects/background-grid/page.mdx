
import BackgroundGrid from '@/showcase/effects/BackgroundGrid'

export const metadata = {
  title: 'Background Grid',
  description: 'Adds a grid pattern to the background of an element or window with a specified color, size, and stroke width.',
}

# Background Grid Effect

<div className="mt-4">Adds a grid pattern to the background of an element or window with a specified color, size, and stroke width.</div>

<ComponentPreview path="effects/BackgroundGrid" preview={
  <BackgroundGrid className="relative w-full h-52 z-10" color="#FB3A5D" size="25px" strokeWidth="3px" />
} />
