import { TextTicker } from '@/showcase/components/text/text-ticker/TextTicker'
import TextCards from '@/showcase/ui-group/TextCards'

export const metadata = {
  title: 'Text',
  description: 'Text is a component that displays text with a text effect and animation.',
}

# Text 
<TextCards/>
