import ToggleCards from '@/components/Cards/ToggleCards'

export const metadata = {
  title: 'Toggle',
  description:
    'Toggle switches are used to quickly switch between two possible states.',
}

# Toggles

<ToggleCards />
