import PreviewCodeTabs from '@/components/CodePreview/page'
import { ThreeDButton } from '@/ui/button'
import Link from 'next/link'
import { ChevronLeft } from 'lucide-react'

export const metadata = {
  title: '3D Button',
  description:
    'A button which has a 3D effect and gives a press feeling when clicked.',
}

<Link className="flex flex-row items-center" href="/docs/components/button">
  <ChevronLeft size={17} />
  Back
</Link>

# 3D Button

<PreviewCodeTabs
  previewContent={
    <div className="group flex w-full items-center justify-center gap-3">
      <ThreeDButton />
    </div>
  }
  codeContent={` 
export const ThreeDButton = () => {
  return (
    <button
      className="group relative m-1 inline-flex cursor-pointer items-center justify-center rounded-lg border-b-2 border-l-2 border-r-2 border-red-700 bg-gradient-to-tr from-red-600 to-red-500 px-4 py-1 text-white shadow-lg transition duration-100 ease-in-out active:translate-y-0.5 active:border-red-600 active:shadow-none"
      onClick={() => console.log('Hello')}
    >
      <span className="absolute h-0 w-0 rounded-full bg-white opacity-10 transition-all duration-300 ease-out group-hover:h-32 group-hover:w-32"></span>
      <span className="relativ font-medium">Syntax UI</span>
    </button>
  )
}
`}
  language="jsx"
/>
