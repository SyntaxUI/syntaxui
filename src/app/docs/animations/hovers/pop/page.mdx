import Link from 'next/link'
import { RequiresTailwind } from '@/components/Requires'
import HoverAnimations from '@/components/HoverAnimationCards'
import { HoverPopButton } from '@/components/PreviewCode/HoverAnimationButtons'
import { ChevronLeft } from 'lucide-react'

<Link
  className="hover: flex flex-row items-center"
  href="/docs/animations/hovers"
>
  <ChevronLeft size={17} />
  Back
</Link>

# Hover Pop

<div className="mt-4"></div>

<Preview className="group">
  <HoverPopButton />
</Preview>

<CodeGroup title="tailwind.config.js">

```js
{
  animation: {
    'hover-pop': 'hover-pop 0.5s ease-out',
  },
 keyframes: {
    'hover-pop': {
         '0%': {
            transform: 'scale(1)',
          },
          '50%': {
            transform: 'scale(1.07)',
          },
          '100%': {
            transform: 'scale(1)',
          },
    },
 }
}
```

</CodeGroup>
