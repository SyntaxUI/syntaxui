import Link from 'next/link'
import { RequiresTailwind } from '@/components/Requires'
import HoverAnimations from '@/showcase/cards/HoverAnimationCards'
import { HoverTadaButton } from '@/showcase/animations/hover/HoverTadaButton'
import { ChevronLeft } from 'lucide-react'
import PreviewCodeTabs from '@/components/CodePreview/page'

<Link className="flex flex-row items-center" href="/docs/animations/hovers">
  <ChevronLeft size={17} />
  Back
</Link>

# Tada

<div className="mt-4"></div>

<PreviewCodeTabs
  previewContent={
    <div className="group flex w-full items-center justify-center">
      <HoverTadaButton />
    </div>
  }
  codeContent={`
  {
  animation: {
    "hover-tada": "hover-tada 1s ease-in-out"
  },
  keyframes: {
    "hover-tada": {
      "0%": {
        "transform": "scale(1)"
      },
      "10%": {
        "transform": "scale(0.9) rotate(-3deg)"
      },
      "20%": {
        "transform": "scale(0.9) rotate(-3deg)"
      },
      "30%": {
        "transform": "scale(1.1) rotate(3deg)"
      },
      "40%": {
        "transform": "scale(1.1) rotate(-3deg)"
      },
      "50%": {
        "transform": "scale(1.1) rotate(3deg)"
      },
      "60%": {
        "transform": "scale(1.1) rotate(-3deg)"
      },
      "70%": {
        "transform": "scale(1.1) rotate(3deg)"
      },
      "80%": {
        "transform": "scale(1.1) rotate(-3deg)"
      },
      "90%": {
        "transform": "scale(1.1) rotate(3deg)"
      },
      "100%": {
        "transform": "scale(1) rotate(0)"
      }
    },
  }
}`}
  language="javascript"
/>
