import Link from 'next/link'
import { RequiresTailwind } from '@/components/Requires'
import HoverAnimations from '@/components/HoverAnimationCards'
import { HoverTadaButton } from '@/components/PreviewCode/HoverAnimationButtons'
import { ChevronLeft } from 'lucide-react'

<Link
  className="hover: flex flex-row items-center"
  href="/docs/animations/hovers"
>
  <ChevronLeft size={17} />
  Back
</Link>

# Tada

<div className="mt-4"></div>

<Preview className="group">
    <HoverTadaButton />
</Preview>
<CodeGroup title="tailwind.config.js">

```js
{
  "animation": {
    "hover-tada": "hover-tada 1s ease-in-out"
  },
  "hover-tada": {
    "0%": {
      "transform": "scale(1)"
    },
    "10%": {
      "transform": "scale(0.9) rotate(-3deg)"
    },
    "20%": {
      "transform": "scale(0.9) rotate(-3deg)"
    },
    "30%": {
      "transform": "scale(1.1) rotate(3deg)"
    },
    "40%": {
      "transform": "scale(1.1) rotate(-3deg)"
    },
    "50%": {
      "transform": "scale(1.1) rotate(3deg)"
    },
    "60%": {
      "transform": "scale(1.1) rotate(-3deg)"
    },
    "70%": {
      "transform": "scale(1.1) rotate(3deg)"
    },
    "80%": {
      "transform": "scale(1.1) rotate(-3deg)"
    },
    "90%": {
      "transform": "scale(1.1) rotate(3deg)"
    },
    "100%": {
      "transform": "scale(1) rotate(0)"
    }
  }
}
```

</CodeGroup>
