import SideInfiniteScroll from '@/showcase/animations/SideInfiniteScroll'
import CodeGroup from '@/components/code/CodeGroup'
import { RequiresTailwind } from '@/components/Requires'
import {
  Table,
  TableBody,
  TableCaption,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from '@/components/ui/table'

export const metadata = {
  title: 'Side Infinite Scroll',
  description: 'A side infinite scroll animation',
}

# Side Infinite Scroll

<PreviewCodeGroup
  preview={
      <SideInfiniteScroll />
  }
  code={`
import { motion } from 'framer-motion'
import { FC } from 'react'

const SideInfiniteScroll: FC = () => {
  const items = [
    <NextJSIcon />,
    <SyntaxUIIcon />,
    <VercelIcon />,
    <TailwindIcon />,
    <FramerMotionIcon />,
  ]
  const transition = { duration: 40, repeat: Infinity, ease: 'linear' }
  return (
    <div className="relative h-20 w-full overflow-hidden ">
      <div className="pointer-events-none absolute -left-1 z-10 h-full w-32 bg-gradient-to-r from-white"></div>
      <div className="pointer-events-none absolute -right-1 z-10 h-full w-32 bg-gradient-to-l from-white"></div>

      <motion.div
        className="absolute left-0 top-0 flex h-20 w-fit gap-20 px-10"
        initial={{ x: '0%' }}
        animate={{ x: '-100%' }}
        transition={transition}
      >
        {items.map((item, index) => (
          <div
            className="flex h-20 w-60 items-center justify-center overflow-hidden"
            key={index}
          >
            {item}
          </div>
        ))}
      </motion.div>
      <motion.div
        className="absolute left-0 top-0 flex h-20  w-fit gap-20 px-10"
        initial={{ x: '100%' }}
        animate={{ x: '0%' }}
        transition={transition}
      >
        {items.map((item, index) => (
          <div
            className="flex h-20 w-60 items-center justify-center overflow-hidden"
            key={index}
          >
            {item}
          </div>
        ))}
      </motion.div>
    </div>
  )
}

export default SideInfiniteScroll
`}
  language="js"
/>
